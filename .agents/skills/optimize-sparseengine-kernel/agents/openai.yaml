interface:
  display_name: "Optimize SparseEngine Kernel"
  short_description: "Optimize SparseEngine GPU kernels end to end"
  default_prompt: "Use $optimize-sparseengine-kernel to find, implement, tune, profile, and integrate a SparseEngine GPU kernel."
